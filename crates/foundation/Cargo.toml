[package]
name = "foundation"
description = "Fair School Funding Plan base cost build-up, per R.C. 3317.011"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
publish.workspace = true

[dependencies]
edfund-core = { path = "../edfund-core" }

[dev-dependencies]
# For examples/input_year_refresh.rs, not for the tests. Cargo resolves dev-dependencies
# for examples/ too, which is the only reason this compiles.
local-capacity = { path = "../local-capacity" }

[lints]
workspace = true
