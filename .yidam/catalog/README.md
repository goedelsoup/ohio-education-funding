# catalog

The catalog tracks data sources. Corpus nodes cite catalog entries as shallow provenance
edges rather than embedding source metadata inline. See
[catalog conventions](../.vendor/prelude/guidelines/directories.md) for node format and
the corpus/catalog relationship.

Provenance carries more weight here than in most domains. A funding figure is only useful
if a reader can tell whether it came from a Department of Education and Workforce payment
report, an LSC estimate made before the fiscal year closed, or a district's own five-year
forecast — three numbers that describe the same thing and routinely disagree. Every numeric
claim in the corpus should reach a catalog entry in one hop.

## Source registry

<!-- REGEN: yidam catalog-audit
Regenerated by: `yidam catalog-audit`
Fields per entry: slug, source type (paper/dataset/API/database/other), description,
                  integrity status (hash present/absent, stale flag),
                  corpus citation count (used-by).
Sorted by: type, then slug.
-->
_Run `yidam catalog-audit` to populate._
<!-- /REGEN -->

## Adding a source

1. Create `catalog/<slug>.md` — use `author-year` for papers, a descriptive slug for
   datasets and APIs (`dew-foundation-payments`, `ohio-tax-abstract`).
2. Include: source name, type, location or access method, one-sentence description of
   what it contains, any access constraints.
3. Optionally add a `used-by` list of corpus node links for reverse traversal.
4. Link from any corpus node that draws on it: `[Author Year](../catalog/<slug>.md)`.
